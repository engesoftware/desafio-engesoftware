# Informações para execução da API - Laravel
- Crie a base de dados: CREATE DATABASE `agenda` /*!40100 COLLATE 'utf8_general_ci' */
- Execute os seguintes comandos:
   - php artisan key:generate
   - php artisan migrate --seed
   - 
